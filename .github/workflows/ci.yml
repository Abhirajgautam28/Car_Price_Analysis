name: Deprecated CI (now merged)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "This workflow is deprecated. See .github/workflows/docker-build-and-push.yml for the merged CI+Docker workflow."
