name: Greetings

on:
  pull_request_target:
    types: [opened, reopened]
  issues:
    types: [opened]
  workflow_dispatch: {}

jobs:
  greeting:
    name: Welcome new contributors
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
      pull-requests: write

    steps:
      - name: Welcome first-time contributors (issues & PRs)
        uses: actions/first-interaction@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          issue-message: |
            👋 Thanks for opening an issue!

            Please add a short description, steps to reproduce, and screenshots if relevant.

            Quick links: CONTRIBUTING.md — https://github.com/${{ github.repository }}/blob/main/CONTRIBUTING.md

            — Gracias / Thank you
          pr-message: |
            👋 Thanks for the pull request!

            Add a brief summary, link any related issue, and ensure tests/linters pass.

            Quick links: README.md — https://github.com/${{ github.repository }}/blob/main/README.md

            — Bienvenido / Welcome
